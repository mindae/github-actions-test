name: shell commands
on: [push]
jobs:
  run-shell-commands:
    runs-on: ubuntu-latest
    steps:
      - name: echo a string
        run: echo 'hello gitactions!'
      - name: mutltiline script commands
        run: |
          node -v
          npm -v
